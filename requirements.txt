# ── App backend ────────────────────────────────────────────────
fastapi>=0.115
uvicorn[standard]>=0.34
pydantic>=2.10
pydantic-settings>=2.6
httpx>=0.28
python-multipart>=0.0.9

# auth + database
SQLAlchemy>=2.0        # SQLite — ML/farm data (plots, scans, ...)
aiosqlite>=0.20
motor>=3.6              # MongoDB — accounts (phone/OTP login)
PyJWT>=2.9

# ── ML core (crop-disease classifier) ─────────────────────────
# CPU wheels: pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
torch>=2.2
torchvision>=0.17
timm>=1.0
grad-cam>=1.5
scikit-learn>=1.3
pillow>=10.0
numpy>=1.26

# ── Module E (GenAI assistant) — optional, offline fallback if unset ──
google-generativeai>=0.8
# Local speech-to-text for the assistant's mic button — runs on this server,
# not a cloud service, so voice input works with no internet at all (only
# the browser <-> this backend round-trip is needed). ~150MB model download
# on first use (see AGRISMART_WHISPER_MODEL_SIZE).
faster-whisper>=1.0

# ── Dev / test ────────────────────────────────────────────────
pytest>=8.2
pytest-asyncio>=0.24
mongomock-motor>=0.0.34  # in-memory Motor-compatible fake — no real MongoDB needed for tests
